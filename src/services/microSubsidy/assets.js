import request from '@/utils/request'
import { stringify } from 'qs'

// 获取列表
export function fetchAssetsList(params) {
  //   return request.get(`/matrix/scm/carbonAccount/list?${stringify(params)}`)
}

// 获取详情
export function getDetail(id) {
  // return request.get(`/matrix/scm/carbonAccount/detail/${id}`)
}
